export interface HouseKeeping {
  room_no: string;
  features: string;
  fd_status: boolean;
  hs_status: number;
  admin_status: number;
  housekeeper: number;
  shift: number;
  linen_status:boolean;
  viewMode: boolean
}
